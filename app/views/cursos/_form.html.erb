<%= form_for(@curso) do |f| %>
  <% if @curso.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@curso.errors.count, "error") %> prohibited this curso from being saved:</h2>

      <ul>
      <% @curso.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :aluno_id %><br>
    <%= f.text_field :aluno_id %>
  </div>
  <div class="field">
    <%= f.label :professor_id %><br>
    <%= f.text_field :professor_id %>
  </div>
  <div class="field">
    <%= f.label :coordenador_id %><br>
    <%= f.text_field :coordenador_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
